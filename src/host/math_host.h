#ifndef MORPHEUS_MATH_HOST_H
#define MORPHEUS_MATH_HOST_H

/* Host-only aggregate interface: fixed-size la values plus checked dynamic
   vectors, matrices, decompositions, solving, and regression. */
#include "la_host.h"
#include "numeric.h"

#endif
